Default to rendered Markdown for all user-facing documentation/plans/specs/tables; never wrap whole docs in fenced code blocks unless the user explicitly asks for raw source,
and always use valid Markdown tables so the UI renders them visually.

Let's adopt this mentality of 'less is more' if the code is not really necessary, remove it or don't write it
