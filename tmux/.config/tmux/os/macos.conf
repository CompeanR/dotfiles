# macOS clipboard integration.
# y yanks and leaves copy mode; Y yanks and stays in copy mode.
bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi Y send -X copy-pipe "pbcopy"
