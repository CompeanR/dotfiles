# Linux clipboard integration.
# Allow nested terminal applications (such as Herdr) to forward OSC 52 writes
# through tmux to the outer terminal's system clipboard.
set-option -s set-clipboard on
bind-key -T copy-mode-vi y send -X copy-pipe "wl-copy"
