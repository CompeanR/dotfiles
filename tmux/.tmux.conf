# Compatibility shim for older tmux versions that only load ~/.tmux.conf.
# Modern tmux also loads ~/.config/tmux/tmux.conf, so the XDG entrypoint guards
# itself to avoid applying the config twice.
source-file ~/.config/tmux/tmux.conf
